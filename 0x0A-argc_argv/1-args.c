#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints number of parameter followe dby a new line;
 * @argc: int
 * @argv: list
 * Return: 0
 */
int main(int argc, char *argv[])
{
    (void)argv;
    printf("%d\n", argc);

    return 0;
}
